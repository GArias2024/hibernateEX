<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <groupId>com.example</groupId>
  <artifactId>hibernate</artifactId>
  <version>0.0.1-SNAPSHOT</version>
  
  
  
  <dependencies>
    <dependency>
        <groupId>com.h2database</groupId>
        <artifactId>h2</artifactId>
        <version>2.1.214</version> <!-- Cambia la versión a la más reciente si es necesario -->
        <scope>runtime</scope> <!-- Solo es necesario en tiempo de ejecución -->
    </dependency>
    
    <dependency>
	    <groupId>org.hibernate</groupId>
	    <artifactId>hibernate-core</artifactId>
	    <version>6.2.11.Final</version> <!-- Cambia a la última versión si es necesario -->
	</dependency>

</dependencies>


<build>
    <plugins>
        <!-- Plugin de compilación para especificar la versión de Java -->
        <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-compiler-plugin</artifactId>
            <version>3.10.1</version> <!-- Asegúrate de usar una versión compatible -->
            <configuration>
                <source>17</source> <!-- Indica la versión del código fuente -->
                <target>17</target> <!-- Indica la versión de bytecode -->
            </configuration>
        </plugin>
    </plugins>
</build>




</project>